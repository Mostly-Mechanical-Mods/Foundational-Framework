# ### DECLARE WAR

action_FF_debug_compat_interaction = {
	type = alert
	priority = 600
	icon = action_PoW_foundational_framework_required
	
	check_create_action = {
		if = {
			limit = {
				is_character_interaction_shown = {
					interaction = FF_debug_compat_interaction
					recipient = root
				}
			}
			try_create_important_action = {
				important_action_type = action_FF_debug_compat_interaction
			}
		}
	}

	effect = {
		open_interaction_window = {
			interaction = FF_debug_compat_interaction
			actor = root
			recipient = root
		}
	}
}